<%= turbo_stream.replace "#{dom_id(@micropost)}_form" do %>
  <%= render "form", react: nil, post: @micropost %>
<% end %>

<%# <%= turbo_stream.replace "notifications" do %>
  <%# <%= render "notifications/show", notifications: current_user.notifications %> %>
<%# <% end %>
