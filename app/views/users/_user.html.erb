<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <%# cho admin xóa, không cho xóa chính mình %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= button_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
  <% end %>
</li>